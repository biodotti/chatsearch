services:
  - type: web
    name: chat-estagio-probatorio
    env: node
    buildCommand: npm ci --production
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      # PORT é provida pelo Render dinamicamente via variável de ambiente
